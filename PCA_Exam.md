---
tags:
  - gcp
---
# Exam
- Cloud Pub/Sub API has not yet been enabled => You should navigate to the APIs & Services section in Google Console and enable Cloud Pub/Sub API.
- The company wants to benefit from Lift and Shift approach => This company should deploy the application to Compute Engine Managed Instance Group with autoscaling enabled based on CPU utilization. ==The Lift and Shift approach is a migration strategy where the applications are moved as they are, without any changes, to the cloud==
- In the context of API error messages, what specific data and information would you suggest including? => An API error message should return a status code form with the standard 400s and 500s HTTP status codes along with additional error details in the payload. ==An API error message is a response that a client receives when an error occurs while making an API request. It is important to provide clear and useful information in the error message to help developers understand what went wrong and how to fix it. Returning an HTTP status code is essential to indicate whether the request was successful or not. Standard HTTP status codes such as 400s and 500s should be used to indicate the type of error that occurred, such as 400 for client-side errors or 500 for server-side errors. In addition to the status code, the API error message should also include additional error details in the payload, such as a message explaining what went wrong, and potentially additional information to help developers diagnose the problem.==
- Several people from your company's compliance department need access to some of this information. As a cloud architect, what should you do to follow Google's best practices? => You should use granular ACLs on the bucket. 
- The platform requires a database solution that supports high transaction throughput, low latency, and scales automatically to handle unpredictable workloads => Implement Cloud Spanner with multi-region configuration.
- Traffic must remain private, and Altostrat wants centralized control over routing, monitoring, and future expansion to additional regions. Which networking design best meets Altostrat’s security and scalability requirements? => Use a Shared VPC with Dedicated Interconnect, centralize connectivity in a host project, and enable Private Google Access for on-premises systems. ==Shared VPC = central network control, Dedicated Interconnect = private high-bandwidth hybrid connection, Private Google Access = private access to Google APIs like Cloud Storage and BigQuery - A private VM needs to upload files to Cloud Storage. You do not want the VM to have a public IP. Solution: Enable Private Google Access on the subnet.==
- Users expect fast and accurate responses. What SLOs should you define to align with user expectations and ensure reliability? => Define one SLO as 99.9% of HTTP requests returning a 2xx status code. Define the other SLO as 95% of queries returning results within 300 ms. ==Ensuring 99.9% of HTTP requests return 2xx status codes aligns with service reliability. Adding a latency goal for 95% of queries provides a realistic measure for application responsiveness.==
- you plan to decommission all existing applications and completely redesign and rewrite them as cloud-native applications. Which approach should you choose? => Remove and Replace ==the goal is to completely redesign and rewrite the legacy applications as cloud-native applications. The "Remove and Replace" approach involves decommissioning the existing applications and building new cloud-native applications from scratch. This approach provides the opportunity to take full advantage of cloud-native technologies and design patterns, and can lead to more efficient and scalable applications that are better suited to the cloud environment.==
	- ❌ Lift and Shift = Rehost , move the app to cloud with minimal changes.
	- ❌ Improve and Move = Refactor / Move and Improve , improve the app while moving it to cloud so it can benefit from cloud features.
	- Remove and Replace = Rebuild / Rip and Replace , throw away the old app and build a new cloud-optimized app.
	
-  The company's primary requirements include scalability, cost-effectiveness, and the ability to process and analyze data in real-time => Dataflow ==Dataflow is a managed service for stream and batch processing.==
- The company plans to modernize its IVR system into a conversational commerce solution that can answer product questions, recommend alternatives, and guide users through checkout in real time. => Build a conversational interface using Dialogflow CX integrated with product discovery and order APIs running on GKE. ==Dialogflow CX enables rich, stateful conversational flows that integrate with real-time product discovery and ordering services, reducing abandonment and agent dependency.==
- Altostrat plans to burst compute-intensive media processing workloads (e.g., video analysis and metadata enrichment) from its on-premises Kubernetes cluster into Google Cloud during peak demand => Use Anthos clusters on-premises and GKE with shared VPC networking and fleet-based workload identity. ==hybrid Kubernetes (Normal demand: Run media processing on-prem, Peak demand: Send extra jobs/workloads to Google Cloud GKE)==
- The company wants to deploy the application in multiple regions to ensure minimal downtime in case of a regional failure. However, the company is also concerned about the costs associated with maintaining a multi-region architecture and seeks to optimize costs while ensuring compliance and availability. Which design should you recommend for the disaster recovery solution? => Deploy the application in two regions with active-active load balancing using Cloud Load Balancing, and use Cloud Spanner for globally consistent data storage. ==This solution is ideal for meeting the company’s needs. By deploying the application in two regions with active-active load balancing, the system can continue to operate even if one region fails, ensuring high availability. Cloud Spanner’s global consistency ensures that data remains synchronized across regions, maintaining data integrity and compliance with regulatory requirements.==
- the application experienced downtime due to a service failure, which had a cascading effect => Implement circuit breakers and retries with exponential backoff and jitter for inter-service communications. ==Circuit breakers can prevent a single service failure from bringing down the whole system by stopping the flow of traffic to the failed service. Retries with exponential backoff and jitter can give the service time to recover, reducing the chances of cascading failure.==
- Development teams want a **AI-driven services and consistent deployment model** that enables fast iteration, minimal infrastructure configuration, and easy promotion between environments. The platform team also wants built-in observability and reduced alerting complexity compared to their current mix of => Use Cloud Run for all stateless services, integrate Cloud Build for CI/CD, and rely on Cloud Monitoring and Logging ==Cloud Run combined with Cloud Build enables fully managed deployments, fast iteration, native observability, and minimal operational overhead—directly supporting Altostrat’s goals.==
	- ❌ Cloud Functions are usually good for small event handlers, but for many modernized services, Cloud Run containers give a more consistent deployment model.
- prepare a migration strategy for a company that wants to migrate all applications from its on-premise data center to GCP. The company's DevOps team currently use Jenkins to automate configuration updates => Jenkins can be delivered using Google Marketplace. ==Cloud Marketplace provides a wide range of pre-configured software solutions, including Jenkins, that can be easily deployed and managed on Google Cloud Platform (GCP). By using Jenkins from the marketplace, the company can quickly provision and configure Jenkins on GCP without the need for manual installation and configuration.==
	 - ❌ App Engine Standard environment is a platform-as-a-service (PaaS) offering that is designed for ==running web applications==, and it has ==limitations in terms of the available runtime and configuration options==, which may not align well with the requirements of running Jenkins.
	 - ❌ Create a YAML Kubernetes Deployment file referencing the Jenkins docker image and deploy to the new GKE cluster: ==While this approach is technically possible, it requires more advanced knowledge of Kubernetes and containerization compared to using a managed solution from the marketplace.==
- video streaming service that needs to support millions of users globally. The service must automatically scale to accommodate sudden increases in user traffic, especially during live events. The architecture involves serving video content from Compute Engine instances, storing metadata in Firestore, and caching frequently accessed content in Cloud CDN => Distribute Compute Engine instances across multiple regions with auto-scaling enabled, and use Firestore in Datastore mode in each region to handle metadata. Cache video content with a global Cloud CDN.
	- ❌ Use Cloud Run to deploy the video streaming service: ==While Cloud Run offers auto-scaling based on request count, it may not be suitable for the high-performance needs of a video streaming service due to potential cold start latency.==
- You need to design a resource hierarchy that allows you to apply company-wide policies, segregate resources at the department level, and isolate resources at the team level. What would be the best way to set up your GCP resource hierarchy => Create an organization for the company, a project for each team, and segregate departments using folders. ==The organization node is the root node in the Google Cloud resource hierarchy and represents your company. Under the organization, creating folders for each department and then projects for each team would allow you to apply company-wide policies, segregate resources at the department level, and isolate resources at the team level.==
	- ❌ Create an organization for the company, a project for each department, and folders for each team: ==Folders should not be created under projects in GCP resource hierarchy. Folders can only be created under an organization or other folders.==
- They want to reduce operational toil, improve incident response, and ensure teams are learning from failures => Implement automated monitoring and alerting with well-defined SLOs and error budgets
- Your marketing team uses Looker dashboards connected to a BigQuery dataset with billions of rows. Users report long load times when filtering by date or campaign. As a cloud architect, you're asked to improve dashboard performance and cost-efficiency, especially for frequently viewed reports. What should you do? => Use Looker Persistent Derived Tables (PDTs) scheduled during off-peak hours to pre-aggregate data ==PDTs allow you to materialize complex queries in BigQuery and schedule them during off-peak hours, improving performance and reducing query cost. Users query the precomputed result, making dashboards faster without sacrificing accuracy.==
- You need to ensure that billing reports can be generated per department, and resources can be isolated per team => Create a folder for each department and then create projects under those folders for each team. ==Billing can be set up at the project level, allowing you to generate billing reports per department.==
- You have to push frequent updates to this application without causing any downtime or disrupting the user experience => Implement a rolling update strategy with readiness and liveness probes. ==readiness and liveness probes will ensure that the new Pods are ready to serve traffic and are healthy before they replace the old ones== 
	- ❌ Implement a canary deployment strategy: ==Canary deployments release the new version to a small subset of users before rolling it out to everyone. This is useful for testing new features with a smaller audience before a full release. While canary deployments can be useful in many scenarios, they don't guarantee zero-downtime by themselves and can be more complex to set up and manage.==
- High availability, Scalability (handle sudden spikes in traffic) , User engagement (User engagement means how actively users interact with and use the service.) => Uptime percentage, traffic scalability (requests per second during peak times), and user engagement metrics such as average session duration and content interaction rates.
- allowing customers to search for products using natural language queries such as “lightweight running shoes for trail running under $150”. => Use Vertex AI Search for Retail to index the product catalog and integrate it with the web and conversational channels via APIs ==Vertex AI Search for Retail is purpose-built for retail product discovery, supports natural language queries, semantic search, and relevance tuning, and integrates easily with existing applications via managed APIs—minimizing operational overhead.==
	- ❌ Deploy a custom NLP model on GKE, trained on product descriptions stored in Cloud Storage ==Managing custom NLP models on GKE increases operational complexity and maintenance burden, conflicting with Cymbal’s goal of modernization and reduced manual effort.== 
- A social media application allows users to upload pictures. You need to convert each image to your internal optimized binary format and store it. As a cloud architect, you want to use the most efficient, cost-effective solution. What should you recommend? => You should save uploaded images in a Cloud Storage bucket, and monitor the bucket for uploads. Run a Cloud Function to convert the images and to store them in a Cloud Storage bucket.
	- ❌ You should store uploaded images in Filestore, monitor Filestore entries, and then run a Cloud Function to convert the images and store them in Filestore. ==Filestore is a managed file storage service for applications that need a file system interface. While it can store binary files like images, it may not be the most efficient and cost-effective solution for storing and processing images compared to using Cloud Storage.==
	- ❌ You should store uploaded images in Firestore, monitor Firestore entries, and then run a Cloud Function to convert the images and store them in Firestore. ==Firestore is a flexible, scalable NoSQL document database, but it is primarily designed for storing structured data rather than binary objects like images. Storing and processing images in Firestore may not provide the optimal performance and cost-effectiveness compared to using Cloud Storage.==
- frontend tier should only communicate with the backend tier, and the backend tier should only communicate with the database tier. All inter-tier traffic must follow the principle of least privilege => Use network tags to assign each tier and configure firewall rules to allow specific traffic flows.
- implement a highly available and scalable NoSQL solution for its e-commerce platform => Use Google Kubernetes Engine for the e-commerce platform with autoscaling and use Cloud Firestore for storage.
- plan to fine-tune Gemini and other foundation models, compare their performance, and then deploy the best-performing model as a REST endpoint for downstream applications => Use Vertex AI Model Garden to explore and fine-tune models, and deploy the best one using Vertex AI Endpoints. ==Vertex AI Model Garden provides a catalog of pre-trained and foundation models, including Gemini, that can be fine-tuned using enterprise data. Once trained, models can be deployed with Vertex AI Endpoints for scalable inference. This workflow supports evaluation, monitoring, and version control, aligning perfectly with MLOps best practices.==
- After deployment, instances are repeatedly terminated and restarted. Health check logs indicate a "connection timeout" error when trying to reach the backend =>Ensure the backend service is configured with the correct instance group and appropriate health check settings, and allow traffic from health check IP ranges via a firewall rule. ==The "connection timeout" error indicates that the health check is unable to reach the backend instances, likely due to a missing or misconfigured firewall rule. Ensuring the backend service has the correct instance group and health check configuration, along with a firewall rule to allow traffic from the health check IP ranges, resolves the issue.== 
- the front-end services are hosted in a Virtual Private Cloud (VPC) in region A and the back-end services are hosted in a separate VPC in region B. You need to enable communication between the front-end and back-end services => Set up VPC Peering between the two VPCs.
- The firm has an SLA of 99.99% uptime and your application should be available all the time. The application also has varying loads throughout the day and spikes during the holiday season => Deploy the application on Kubernetes Engine (GKE) with clusters in **multiple regions and with autoscaling enabled.**
	- ❌ While **App Engine Standard can auto-scale and meet high load requirements**, it's less suited for complex microservices-based applications. It also doesn't inherently provide multi-region redundancy.
- The application is expected to experience heavy traffic, so it needs to be highly scalable and resilient => Deploy the web front end and REST API backend on separate App Engine services, and the database on Cloud Spanner.  
	- ❌ Deploy the entire application on a **single GKE cluster**, using different namespaces for the front end, the backend, and the database.
- EHR wants to adopt the BeyondCorp access model and ensure that only authorized users with a Google Workspace account can access the collaboration tool. Additionally, EHR must verify device trust levels before granting access  => Deploy an Identity-Aware Proxy (IAP) and configure it to enforce device trust and user authentication for the collaboration tool.
- connection between Google Cloud and your on-premises infrastructure => You should create a network plan for your VPC in Google Cloud that uses non-overlapping CIDR ranges with your on-premises environment. Use a Cloud Interconnect connection between your on-premises environment and Google Cloud.
- you are tasked with implementing a testing strategy that includes the use of cloud emulators to ensure each service is properly tested in isolation before integration => Configure separate CI/CD pipelines to include stages that set up and tear down emulators for each service, ensuring isolated environment testing during development.
- You aim to migrate 2% of your users to the new version while minimizing complexity => You should deploy a new version in the same application and split the traffic (98% to 2%).
- The solution must reduce call center load, provide accurate product recommendations across a large catalog, and integrate with existing Kubernetes-based services. Cymbal also wants to avoid managing NLP infrastructure and prefers a fully managed, scalable solution that can evolve over time => Use Dialogflow CX integrated with Cloud Functions and BigQuery to provide conversational product search and recommendations ==Dialogflow CX provides a managed conversational AI platform, integrates natively with serverless backends, scales automatically, and supports rich, engaging customer interactions aligned with Google Cloud best practices.==
- build a global video streaming platform on GCP , The content is primarily video-on-demand (VOD), and the company wants to implement a solution that optimizes the delivery of videos based on the user's location , support real-time analytics to monitor viewer engagement and optimize content delivery => Implement the platform using App Engine Standard Environment, Cloud Spanner for metadata storage, and Cloud CDN for video delivery. Store videos in a multi-regional Cloud Storage bucket.
	- ❌ Use Google Kubernetes Engine (GKE) with multi-regional clusters, Cloud CDN for video delivery, and BigQuery for real-time analytics. Store videos in a single-region Cloud Storage bucket. ==GKE with multi-regional clusters and Cloud CDN is a strong combination for global content delivery, but storing videos in a single-region Cloud Storage bucket could result in higher latency for users far from the storage region. Additionally, storing data in a single region introduces a single point of failure, which goes against the high availability requirement. BigQuery is an excellent choice for real-time analytics, but the storage strategy needs improvement.==
- The platform must handle high traffic during sales events, ensure zero downtime, and offer a consistent user experience globally. The business requires the application to be highly available, automatically scalable, and capable of handling localized traffic efficiently. Additionally, the platform needs to integrate with legacy systems hosted on-premises for inventory and order management. The company also wants to optimize costs by paying for resources based on demand. => Implement the application using Google Kubernetes Engine (GKE) with multi-regional clusters, Cloud Spanner for the database, and Cloud Interconnect for high-bandwidth on-premises connectivity. ==based on demand: It means the architecture should scale up during sales events and scale down when traffic drops.==
-  The data warehouse needs to handle several petabytes of data, with frequent, unpredictable spikes in query activity => Use BigQuery for both data storage and analysis.
- remote location that is over 100 kilometers away from a Google-owned point of presence (POP). Budget constraints prevent them from acquiring new hardware => Provision a Partner Interconnect connection. ==It allows the customer to lower latency by connecting directly to a partner network that is directly connected to Google.==
- Highly variable traffic with unpredictable spikes, Heavy analytical read queries during business hours, Strict RPO of near zero, Need to optimize storage cost while maintaining high durability => Deploy a regional AlloyDB cluster with autoscaling read pool nodes and rely on managed distributed storage. ==Regional clusters provide synchronous replication (near-zero RPO). Read pool autoscaling handles traffic spikes efficiently. AlloyDB’s distributed storage layer automatically scales and optimizes storage durability and performance.==
-  Your company plans to migrate a multi-petabyte data set to the cloud. The data set must be available 24hrs a day. Your business analysts have experience only with using a SQL interface. => Load data into Google BigQuery ==BigQuery is a fully managed, serverless data warehouse designed for multi-petabyte-scale datasets with 24x7 availability. It supports standard SQL, which aligns directly with the analysts’ existing skills, and is optimized for fast analytical queries across massive volumes of data without infrastructure management.==
- There are 1000 meeting rooms across 5 offices on 3 continents. Each room is equipped with a motion sensor that reports its status every second. The data from the motion detector includes only a **sensor ID and several different discrete items of information.** => Relational ==because the data needs to be analyzed together with related structured data.==
- You write a Python script to connect to Google BigQuery from a Google Compute Engine virtual machine. The script is printing errors that it cannot connect to BigQuery. => Run your script on a new virtual machine with the BigQuery access scope enabled. ==Access scope on a Google Compute Engine VM controls which Google Cloud APIs the VM is allowed to call using its attached service account , On GCE VMs, access to Google Cloud APIs depends on two things: ( IAM permissions (service account roles), Access scopes configured on the VM.) Even if the service account has BigQuery permissions, the VM can still fail to access BigQuery if the BigQuery API scope was not enabled when the VM was created.==
- Your application needs to process credit card transactions. You want the smallest scope of Payment Card Industry (PCI) compliance without compromising the ability to analyze transactional data and trends relating to which payment methods are used. => Create a tokenizer service and store only tokenized data ==Tokenization reduces PCI scope because your main systems store and analyze tokens instead of actual credit card data. You can still analyze payment methods and transaction trends without exposing sensitive card details.==
- You want to optimize the performance of an accurate, real-time, weather-charting application. The data comes from 50,000 sensors sending 10 readings a second, in the format of a timestamp and sensor reading. Where should you store the data? => Bigtable ==as Bigtable is optimized for massive scale , low latency , time-series workloads , IoT/sensor data ingestion, Cloud SQL would not scale efficiently for this write volume, BigQuery is better for analytics than real-time ingestion, and Cloud Storage is object storage, not optimized for real-time querying.== 
- A small number of API requests to your microservices-based application take a very long time. You know that each request to the API can traverse many services. You want to know which service takes the longest in those cases. => Instrument your application with Stackdriver Trace in order to break down the request latencies at each microservice
- deployed using a least privilege model with separation of duties for administration between production and development resources. => Create a single G Suite account to manage users with each stage of each application in its own project
- the new database server stops responding to SSH connections. It is still serving database requests to the application servers correctly. => Take a snapshot of the disk and connect to a new machine to investigate , Check inbound firewall rules for the network the machine is connected to , Print the Serial Console output for the instance for troubleshooting, activate the interactive console, and investigate
- How should the test coverage differ from their existing backends on the other platforms? => Tests should scale well beyond the prior approaches
- wants to design their solution for the future in order to take advantage of cloud and technology improvements as they become available. => Store as much analytics and game activity data as financially feasible today so it can be used to train machine learning models to predict user behavior in the future ,,,, Begin packaging their game backend artifacts in container images and running them on Kubernetes Engine to improve the availability to scale up or down based on game activity.
	- ❌ Set up a CI/CD pipeline using Jenkins and Spinnake ==use Cloud Build over Jenkins for a GCP-native solution==
- should you deploy to ensure guaranteed-once FIFO (first-in, first-out) delivery of data? => Cloud Pub/Sub to Cloud DataFlow ==Cloud Pub/Sub alone does not guarantee strict FIFO or exactly-once processing by itself. Dataflow can help process streaming data with ordering/deduplication logic so the legacy API receives ordered, non-repeated events.==
- load 7 years of archived .csv files totaling 900 TB of data and then continue loading 10 TB of data daily. You currently have an existing 100-MB internet connection.  => Lease a Transfer Appliance, upload archived files to it, and send it, and send it to Google to transfer archived data to Cloud Storage. Establish a connection with Google using a Dedicated Interconnect or Direct Peering connection and use it to upload files daily.
- You want to minimize latency for all your customers => Save the files in a Multi-Regional Cloud Storage bucket. **Regional** - data stored in a single region. Lowest cost, lowest latency within that region, but high latency for users far away. **Dual-region** - data replicated across two specific regions. **Multi-region** - data replicated across a large geographic area (e.g., us, eu, asia). Higher availability and lower latency for geographically distributed users.
- HTTP status codes of 5xx and 429. => Implement retry logic using a truncated exponential backoff strategy. ==5xx errors — server-side errors , 429 Too Many Requests — rate limiting. The client is sending requests faster than the service allows and needs to slow down==
- test a disaster plan for a mission-critical application => Use Deployment Manager to automate service provisioning. Use Stackdriver to monitor and debug your tests.
- global load balancing based on the URL path being requested. You need to ensure operations reliability and end-to-end in-transit encryption  => Create an HTTPS load balancer with URL maps. ==You need an HTTP(S) Load Balancer because it works at Layer 7 and understands HTTP URLs , User → HTTPS → Load Balancer → HTTPS → Backend==
- All queries on BigQuery need to be billed on a single project. You want to make sure that no query costs are incurred on the projects that contain the data. Users should be able to query the datasets, but not edit them. => Add all users to a group. Grant the group the roles of BigQuery jobUser on the billing project and BigQuery dataViewer on the projects that contain the data. ==roles/bigquery.jobUser: allows users to run query jobs there, roles/bigquery.dataViewer: allows users to read/query the tables only==
	- Billing/query project: where users run BigQuery jobs, this project pays for the query cost
	- Data projects: where datasets/tables are stored, should not pay for query jobs
- You need to set up Microsoft SQL Server on GCP. Management requires that there’s no downtime in case of a data center outage in any of the zones within a GCP region. =>  Set up SQL Server Always On Availability Groups using Windows Failover Clustering. Place nodes in different zones.  ==Using SQL Server Always On Availability Groups + Windows Server Failover Clustering gives SQL Server-level high availability and failover==
-  "scales to zero" so you don’t incur costs when there is no activity.  =>  Cloud Functions ==App Engine Flexible does not scale to zero. It usually keeps at least one instance running== 
	- Cloud Functions       → scales to zero
	- Cloud Run             → scales to zero
	- App Engine Standard   → can scale to zero
	- App Engine Flexible   → does not scale to zero
	- Compute Engine        → does not scale to zero automatically
	- GKE                   → not the simplest scale-to-zero answer
- 